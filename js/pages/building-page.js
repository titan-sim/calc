function renderBuildingPage(container) {
  container.innerHTML = `
    <div class="card">
      <h2>건물</h2>
      <p style="color:var(--text-sub); font-size:14px;">준비 중입니다. 다음 단계에서 만들 예정이에요.</p>
    </div>
  `;
}
