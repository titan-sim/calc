function renderArenaPage(container) {
  container.innerHTML = `
    <div class="card">
      <h2>아레나</h2>
      <p style="color:var(--text-sub); font-size:14px;">준비 중입니다. 5:5(전열 2 / 후열 3) 로직은 추후 추가돼요.</p>
    </div>
  `;
}
